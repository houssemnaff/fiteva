import 'package:flutter/material.dart';

class WorkoutColors {
  // 🏋️ Gym / Salle — force, discipline, performance
  static const salle = Color(0xFF1C4D30); // deep forest (core brand)

  // 🏠 Home workout — calme, accessible, soft energy
  static const maison = Color(0xFF4F7D66); // muted sage green

  // 💃 Dance — énergie fluide, mais sans rose → énergie “motion”
  static const dance = Color(0xFF2E6F5E); // teal-green dynamic

  // 🧘 Recovery — reset, respiration, relaxation
  static const recuperation = Color(0xFF7ABB98); // soft fresh green

  // 🤰 Pregnancy — sécurité, douceur, nature (remplace rose)
  static const grossesse = Color(0xFFA7B8AD); // sage neutral calm

  // 🎯 Zone training — focus, mental sharpness
  static const zone = Color(0xFF2F3E46); // deep charcoal green-blue
}